{
  "name": "meteograph-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Black-box UI tests (Appium + UiAutomator2). No Flutter dependency.",
  "scripts": {
    "test": "wdio run ./wdio.conf.js",
    "appium": "APPIUM_HOME=./.appium appium --base-path / --log-timestamp",
    "driver:install": "APPIUM_HOME=./.appium appium driver install uiautomator2@4.2.9"
  },
  "devDependencies": {
    "@wdio/cli": "9.28.0",
    "@wdio/local-runner": "9.28.0",
    "@wdio/mocha-framework": "9.28.0",
    "@wdio/spec-reporter": "9.28.0",
    "appium": "3.5.0",
    "webdriverio": "9.28.0"
  },
  "//uiautomator2": "Driver installed via `npm run driver:install` (uiautomator2@4.2.9 — last 4.x; 5.x+ require Appium 3). Not an npm dependency; lives under APPIUM_HOME=./.appium."
}
